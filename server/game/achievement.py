def check_achievement(achievement):
    req=return_achievement_req(achievement)
    if req.type == "LEVEL":

    if user.level .req.LEVELTYPE ==req.LevelType:


    if req.type == "SINGLE":
        SWITCH CASE MIT ALLEN ACHIEVMENTS
    set_achievements_at user
def